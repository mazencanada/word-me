/**
 * Global Variables.
 */
let arrayOfWords = [];
let wordsToLearn = [];


/**
 * This function should calculate number of words given to it through a string.
 * This will mainly be used for the textarea for the user's input.
 */
function checkNumberOfWords(string){
    let counter = 0;

    // 1. Split this string into an array of words, and store it in the global variable arrayOfWords.
    // 2. Check the type of each word by looping through the array (String vs Number).
    // 3. If string then, increment the counter.


    return counter;
}


/**
 * This function should return true or false, based on the uniqueness of the given word compared against the
 * global variable arrayOfWords.
 */
function checkIfUnique(word){
    // Write some logic that will loop through the whole arrayOfWords, to check if the current "word" is unique.
}


/**
 * This should update the global array : "arrayOfWords" with the new words.
 * Words can be removed or added to it.
 */
function updateArrayOfWords(word, action){
    // 1. If the action is "add", then add the word to the array.
    // 2. If the action is "remove", then remove the word from the array.
}


/**
 * This function should update the value(s) of the localStorage.
 * It takes a word, and an action : Add or Remove.
 * If Add, then this word needs to be added to the local storage, and vice versa.
 */
function updateWordsToLearn(word, action){
    // 1. Get the current value of the localStorage.
    // 2. Update the value of the global variable wordsToLearn (add or remove).
    // 3. If the word was there before, then do not add it again.
    // 4. Update the value of the localStorage.
}



/**
 * This function should return an alphabetically sorted array of words.
 * @param array
 */
function sortAlphabetically(array){
    let arraySorted = [];
    // Write some logic that will sort the array alphabetically.

    return arraySorted;
}


/**
 * This should generate or show a button in the "Words Generated List".
 */
function showAWordsGeneratedButton(word){
    // Write some logic to show a button.

    // Attach an event listener to the button, when user clicks on heart icon.


    // Attach en event listener to the button, when user clicks on the word.

}

/**
 * Procedures.
 */

// 1. User clicks on generate button.
document.getElementById("generate-button").addEventListener("click",function(){
    // Check if there is any content in the textarea.
    let textAreaValue = document.getElementById("text-area").value;

    ///////////////////// If yes
        let numberOfWords = checkNumberOfWords(textAreaValue);
        if (numberOfWords > 200){
            // Delete the reset of the words.
        }

        let tempArray = [];
        // loop through all the words in arrayOfWords, and check if each word is unique or no, then add it to tempArray.
        arrayOfWords = tempArray;

        // Show the words in the "Words Generated" section, by looping through the arrayOfWords and calling the function showAWordsGeneratedButton().




    ///////////////////// If no.
    // Show the error message.
})

// My name is Mazen Canada and I live in Canada
